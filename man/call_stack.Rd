% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_stack.R
\name{call_stack}
\alias{call_stack}
\title{Build the call tree of a function}
\usage{
call_stack(fn, max_depth = Inf)
}
\arguments{
\item{fn}{Character string naming a function, e.g. \code{"dplyr::select"},
\code{"pkg:::internal_fun"}, or \code{"mean"}.}

\item{max_depth}{Maximum recursion depth. Default \code{Inf}, which follows
every call down to a primitive or a cycle already visited.}
}
\value{
An object of class \code{call_stack}: a named list with \code{name} (the
function name) and \code{calls} (a list of child nodes with the same shape).
}
\description{
\code{call_stack()} takes the name of a function, optionally namespaced
(e.g. \code{"dplyr::select"}), statically parses its body, and recursively
resolves every function it calls, building a tree of those calls, the
calls those functions make, and so on.

Resolution is static (read from the function's source code), not a
runtime trace: the function is never called. \code{UseMethod()} dispatch is
resolved by looking up the generic's registered S3 methods and
following each one. Other dynamic calls (\code{NextMethod()}, S4 and R6
dispatch, non-standard evaluation) show as leaves rather than being
followed further.
}
\examples{
call_stack("mean.default", max_depth = 1)

# a generic: UseMethod() is replaced by the methods it dispatches to
call_stack("mean", max_depth = 1)

}
\seealso{
\code{\link[=print.call_stack]{print.call_stack()}} for how the tree is drawn.
}
