% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_tree_app.R
\name{call_tree_app}
\alias{call_tree_app}
\title{Build the call tree of a running Shiny app}
\usage{
call_tree_app(
  app,
  app_fun = "app",
  ui_fun = NULL,
  server_fun = NULL,
  modules_only = FALSE
)
}
\arguments{
\item{app}{A \code{shiny.appobj}, as returned by \code{\link[shiny:shinyApp]{shiny::shinyApp()}} or
\code{\link[shiny:shinyAppDir]{shiny::shinyAppDir()}}. Not the \code{ShinyAppHandle} returned by
\code{\link[shiny:startApp]{shiny::startApp()}}.}

\item{app_fun}{Character scalar; label used for the root node. The app
object doesn't store the name of its own launch function, so this is
display text only. Default \code{"app"}.}

\item{ui_fun}{Character scalar or \code{NULL}; name of the UI function. When
\code{NULL} (the default), \code{call_tree_app()} tries to recover it
automatically, which only works when \code{ui} was passed to
\code{shiny::shinyApp()} as an unevaluated function
(\code{shinyApp(ui = app_ui, ...)}). If \code{ui} was pre-evaluated
(\code{shinyApp(ui = app_ui(), ...)}), supply the name explicitly so the UI
branch is included.}

\item{server_fun}{Character scalar or \code{NULL}; name of the server
function. When \code{NULL} (the default), the name is inferred by
comparing function bodies against those visible in the server's
environment.}

\item{modules_only}{Logical; see \code{\link[=call_tree_dir]{call_tree_dir()}}. Default \code{FALSE}.}
}
\value{
An object of class \code{call_stack} (see \code{\link[=call_stack]{call_stack()}}).
}
\description{
\code{call_tree_app()} inspects a \code{\link[shiny:shinyApp]{shiny::shinyApp()}} object at runtime,
rather than parsing source files on disk. It extracts the server
function, collects every function visible in the server's enclosing
environment chain, and builds the call graph among them, in the same
shape \code{\link[=call_stack]{call_stack()}} returns.

\code{call_tree_app()} never calls into the shiny package itself; it only
inspects the structure of the \code{shiny.appobj} you pass in, so stackcallr
does not need shiny installed unless you're building one.

This only works on a \code{shiny.appobj}. It can't inspect the
\code{ShinyAppHandle} returned by \code{\link[shiny:startApp]{shiny::startApp()}}: that handle is a
runtime control object (\code{stop()}/\code{status()}/\code{url()}/\code{result()}) for an
app that may be running in a separate process, and it exposes no
reference back to the app's UI, server, or environment. Pass the
\code{shiny.appobj} itself, before calling \code{startApp()} on it.
}
\examples{
\dontrun{
app <- shiny::shinyApp(ui = app_ui, server = app_server)
call_tree_app(app)
call_tree_app(app, modules_only = TRUE)
}

}
\seealso{
\code{\link[=call_tree_dir]{call_tree_dir()}} for the file-based equivalent.
}
