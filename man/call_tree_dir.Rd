% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_tree_dir.R
\name{call_tree_dir}
\alias{call_tree_dir}
\title{Build the call tree of every function defined in a directory of R files}
\usage{
call_tree_dir(path = "R", root, modules_only = FALSE)
}
\arguments{
\item{path}{Character scalar; directory containing \code{.R} files. Default
\code{"R"}.}

\item{root}{Character scalar; name of the function to root the tree at.
Must be one of the top-level functions found in \code{path}.}

\item{modules_only}{Logical. When \code{TRUE}, prune the tree to \code{root} plus
functions that call \code{\link[shiny:NS]{shiny::NS()}} or \code{\link[shiny:moduleServer]{shiny::moduleServer()}} (Shiny
modules); everything else is spliced out, promoting its children so
modules reached through a non-module wrapper function stay visible.
Default \code{FALSE}, which keeps every locally-defined function reachable
from \code{root}.}
}
\value{
An object of class \code{call_stack} (see \code{\link[=call_stack]{call_stack()}}).
}
\description{
\code{call_tree_dir()} parses every \code{.R} file in \code{path} without evaluating any
of it, extracts each top-level function definition
(\code{name <- function(...) ...}), and builds the call graph among those
locally-defined functions. The result is the tree of calls reachable
from \code{root}, in the same shape \code{\link[=call_stack]{call_stack()}} returns, so it prints the
same way.

Unlike \code{\link[=call_stack]{call_stack()}}, which resolves an already-loaded function through
R's own namespace lookup, \code{call_tree_dir()} never loads or runs the
scanned files. That makes it usable on a package's \verb{R/} directory, or a
Shiny app's source, without installing or sourcing it first.
}
\examples{
\dontrun{
call_tree_dir("R", root = "launch_app")
call_tree_dir("R", root = "launch_app", modules_only = TRUE)
}

}
\seealso{
\code{\link[=call_tree_app]{call_tree_app()}} for the equivalent on a running Shiny app.
}
